# Database (used by API)
DATABASE_URL=postgresql://market:market@localhost:5432/market_health

# Redis (used by API for BullMQ + cache)
REDIS_URL=redis://localhost:6379

# API
API_PORT=3000
NODE_ENV=development

# Frontend (used by Next.js)
NEXT_PUBLIC_API_URL=http://localhost:3000

# Data providers (never expose to client)
FRED_API_KEY=
TWELVE_DATA_API_KEY=
ALPHA_VANTAGE_API_KEY=
SENDGRID_API_KEY=
SENDGRID_FROM_EMAIL=

# Optional: DXY proxy if using paid provider for macro.dxy
# Default: FRED broad USD index or leave empty to disable DXY
